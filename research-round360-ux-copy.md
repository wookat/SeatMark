# 第 360 轮调研：用户视角任务流走查 + 去 AI 味专项复核

生产 www.seatmark.cn（部署判据：x-seatmark-rev=r359、首页 bundle=index-D42fBey7.js、x-seatmark-storage=blob），匿名、未注册未登录，Playwright 连接驻留 Chrome CDP（29229）新建独立 context 走查 + curl + 源码对照实证；测试 context 均为临时 incognito 式 context、用完即关，不落站点存储。**本轮只调研不改代码。**

证据分级：【实证】= 生产环境直接操作/观察；【代码】= 源码定位；【推断】= 未直接验证。

## 一、上轮（r359）发现复核

| 上轮发现 | 本轮结果 |
|---|---|
| P2 /seating 名单修改静默清空手工排座 | 【实证】已修。随机排座→手动换座→textarea 改名（学生01→学生一），手工安排原位保留；改动有 toast 明细（名单整体更换 / 新增 N 人排到末尾 / 移除 N 人原位留空 三类语义齐全） |
| P2 /seating 多考场 | 【实证】已修。粘贴含「考场」列名单 → 出现考场筛选下拉，选中只排该考场；handoff 到 /studio 只带该考场 15 行（sessionStorage 核实 headers=姓名/座位号/排/列/班级/考场、rooms={考场02}）；导出文件名附考场号；EN 侧 "Room" 表头同样识别（下拉文案全英） |
| P2 /banquet 排斥对 | 【实证】已修。不同桌面板可加「张伟 ✕ 李娜」等配对，自动分配后两对均落在不同桌（实测 张伟1号桌/李娜5号桌、王芳3号桌/刘洋1号桌）；反向重复添加被去重；宾客删除同步清对；导出前检查弹窗有「排斥冲突 N 对」专节；清单存 localStorage 刷新后仍在 |
| P3 /seating 无撤销 | 【代码】仍缺。全页无 undo；但 /banquet 已有「清空/切预设后 toast 10 秒撤销」模式（BanquetView.vue:383/405 + utils/banquet.ts 快照），模式现成可平移 |
| P3 /seating 无「按列填充」/固定座位 | 【代码】仍缺。FILL_OPTIONS 仍只有 按行填充 / S 形蛇形；座位级锁定（固定某生、封空位）无 |
| P4 EN 拼写、AI 弹窗中文、「利器/大幅提升」 | 【代码】均已修且保持 0 命中 |
| P4 「免费批量生成」尾、quickStart note | 【代码】仍在：seoDescription 含「免费批量生成/免费生成」208/225；quickStart「先看成品效果…」×52（原 ×53，微调 1 处） |
| P4 打印校准入口发现性 | 【代码】仍在（工具栏次级按钮，无首打引导） |
| P4 title 关键词堆叠 | 【实证】仍在（记录在案） |
| Studio 768/1024 工具栏 ≤2 行（顺延项） | 【实证】仍未达标且比上轮记录更挤：768px 实测约 5 行（徽标行 / 适应宽度+显示选项 / 打印·矢量PDF / 图片版PDF+今日剩余 / 图片PNG），1024px 约 3 行 |

## 二、本轮新发现（按严重度）

### 缺陷

1. **【实证+代码·P2】/seating 无表头名单首行含「考场/座位/宿舍」类词 → 首名学生静默丢失。**
   粘贴 `学生01\t男\t考场01` ×30 行（无表头）→「已输入 29 名」「已跳过表头行：学生01、男、考场01」，学生01 从名单与座位图中消失（座位图搜索不到）。根因：`parsePastedRoster`（utils/excel.ts:207,247）的 `HEADER_KEYWORDS` 含未锚定子串「考场/座位/学号/宿舍/桌号/组别/单位/学校/手机…」，无表头名单第一行任一单元格的值含这些词（考场01、座位12、宿舍302、桌号5 均为合法数据值）即整行被当表头。与 r357 的 `student` 未锚定属同族缺陷——那轮修了 SeatingView 自己的 ROSTER_HEADER 快路径，共享解析器的兜底自动判定仍漏。**真实场景**：老师从 Excel 直接复制「姓名+考场」两列无表头数据即触发。**建议**：HEADER_KEYWORDS 改锚定（如 `^考场(号)?$`、`^座位(号)?$`），或改判定为「首行≥2 格命中关键词才当表头」，或给 /seating 名单区补一个「首行是表头」开关（/studio 粘贴弹窗已有此先例）。

2. **【实证·P2】考场筛选切换丢弃该考场已排的座次。**
   考场01 随机排座 → 切到考场02 → 切回考场01 → 座次回到名单顺序（arranged 单实例，不切考场缓存）。好在有 toast 明示「已切换考场，座位已按名单顺序重排」，不是静默丢——但多考场老师的真实流程是逐考场排好再统一导出，现在必须「排一个、导一个」，中途回看前一场就丢。**建议**：arranged 改为按考场键值的 map（`{考场01: [...], 考场02: [...]}`），或至少在目标考场已有安排时提示「切换将放弃本场当前排座」。

### 新需求候选

3. **【推断·P3】宴会「扫码查座」—— guides 自己承认缺口，且隐私理由已被站内技术事实推翻。**
   guidesRound7.ts 婚宴教程多处写「暂无扫码查座」，理由是「需要把名单放到服务器、与名单不上传冲突」。但站内已有零后端先例：模板分享把模板 deflate+base64url 进 `#tpl=` hash（share.ts），全程无请求。同样的做法可把「排好的座次图+宾客速查」编码进链接 hash 生成二维码——宾客手机打开链接在本地渲染只读查座页，名单不出这条链接。中小型婚宴（≤200 人）deflate 后体积在 URL/二维码容量内。**注**：这是对 guides 自述缺口的正面回应，属产品新需求而非缺陷。

4. **【推断·P3】/seating 座位级控制仍空白**：固定某生到某座（讲台旁/前排）、封某座不排人（坏桌、靠门留空）都没有。与「按列填充」同属座位级诉求，可合并为一个「座位操作」迭代。

### i18n / 措辞残留（均 P4）

5. **【实证】考场筛选选项文案冗余**：zh 显示「考场 考场01」、en 显示「Exam room Room 1」——前缀词与值自带词重复。建议选项只渲染值+人数（「考场01 · 15 人」/「Room 1 · 15 students」），或用「全部 / 仅 X」形式。
6. **【实证】首页信任条「0.1mm 排版精度」与全站口径不一致**：首页 TRUST_STATS 硬编码 0.1mm（HomeView.vue:214），而 topicPages/vsPages/教程口径均为「毫米级排版」「校准向导实测 ≤0.35mm」。0.1mm 既无法被 ≤0.35mm 的实测支撑，也是夸量级宣称，建议改「毫米级」或「≤0.35mm」。
7. **【代码】演示样例值口径不一**：多数模板考场示例值用「第N考场」，标准考场版等若干模板 sampleData 用「考场-1」（defaultTemplates.ts:573/988 等），首页精选卡与 /studio 模板卡直接展示「考场-1」，与演示数据集「第1考场」混排出现，观感像「考场减 1」。建议统一为「第N考场」。
8. **【代码】seating→studio handoff 把图表标题塞进「班级」列**（SeatingView.vue:755 `班级: title.value`）：标题若为「期末考试」类活动名，桌贴的班级字段会显示活动名。字段标签本身就叫「班级 / 考场标题」，属弱语义混用，可不改，记录在案。
9. **【实证】定价页降级态文案重复**：账号服务维护期间「账号服务维护中…恢复后可领取」在免费版/专业版/FAQ 内出现 3+ 次，且 FAQ 仍宣称「邮箱注册即送 7 天专业版试用」（当前无法兑现）。属既有降级状态的表现，非新缺陷，建议维护态下 FAQ 对应条目替换为维护口径。

## 三、去 AI 味专项复核（本轮新挖面：口语化营销词表 ×120+、称呼一致性、教程结构、定价页通读）

三道禁词护栏持续有效，本轮新增扫描面结论：**站内文案保持系统性干净，未发现新 AI 腔**。

1. **口语化营销词表 ×120+ 全绿或语境正当**：神器/天花板/宝藏/安利/种草/绝绝子/yyds/破防/拿捏/薅羊毛/白嫖/抄作业/敲黑板/重磅/官宣/揭秘/万字长文/必看/必学/刷爆/爆款/网红/明星同款/ins风/出片上镜/颜值党/小确幸/松弛感/躺平/内卷/上岸/降维打击/吊打/秒杀/史诗级/教科书级/殿堂级/零门槛/有手就行/秒变/秒懂/丝般顺滑/黑科技/硬核/诚意满满/倾力打造/精雕细琢/旗舰/顶配/满配/一劳永逸/传世/典藏/独家首发/震撼来袭/王者归来/千呼万唤/敬请期待 —— 全部 0 命中或纯数据语境（「燃」= 燃面/燃脂/易燃，「万字」= 汉字总量九万，「熬夜」= 痛点场景白描，「同款/大牌」= 物料规格描述，「首选」16 处全部为「政务场合首选黑体」式工具建议）。
2. **称呼一致性**：全站统一「你」体（你的×60+），仅 1 处「您的」在演示物料固定文案（「感谢您的耐心等候」——请柬用语，正当）。
3. **感叹号全站 ×1**（「感谢反馈！」），无滥用。
4. **EN 侧**：seamless/elevate/unlock(营销义)/hassle-free/look no further/game-changer/moreover/furthermore 等 35 词 0 命中（「unlock」3 处为配额解锁功能词）；新增考场筛选、排斥对两处新功能 EN 文案完整（"Exam room"、"Keep apart"、"This pair is already on the list"），无生硬直译。
5. **toast/提示语通读**（52 条去重）：全部短句动宾结构（「已取消导出」「名单已整体更换，座位已按名单顺序重排」「这对排斥已在清单中」），无 AI 腔。
6. **教程正文抽样通读**（考场 5 步篇 + 婚宴 Round7）：结构、口吻与前轮结论一致——具体数字（160g 卡纸、500 人、5% 备卡）、诚实缺口（「暂无扫码查座」）、真实 FAQ 口吻。
7. **残留公式化收束（沿用前轮，未收敛）**：seoDescription「…批量生成」系尾 208/225、quickStart note ×52、「名单不出浏览器」类收束 —— 属功能可信但连读显拼接感的老项，仍建议按场景微调配词，优先级 P4。

## 四、正向确认（本轮实证通过）

- 三 persona 黄金链路全通，pageerror=0；移动端 390px / 、/seating、/banquet、/templates 无文档级横溢。
- /seating 新考场筛选端到端：识别含「考场」表头 → 下拉带人数 → 选中只排该场 → handoff 只带该场 15 行 → 文件名附考场号；EN「Room」表头同样生效。
- /banquet 排斥对端到端：加对 → 自动分配避让 → 反向去重 → 持久化 → 导出前检查列出冲突项。
- /en 全站 chrome 无新增 CJK 残留（仅语言切换「中文」与 ICP 号，属预期）。
- 降级态诚实：授权 503 期间各页明示「账号服务维护中」而非装死。

## 五、排期建议清单

| # | 严重度 | 类型 | 内容 |
|---|---|---|---|
| 1 | P2 | 缺陷 | /seating 无表头名单首行含「考场01/座位12/宿舍302」等词时首名学生被当表头跳过（HEADER_KEYWORDS 未锚定，同族于 r357 student 修复） |
| 2 | P2 | 缺陷 | 考场筛选切换即丢该场已排座次（有 toast 明示但不缓存分场安排）——建议 arranged 按考场分键存储 |
| 3 | P3 | 缺陷 | /seating 无撤销（/banquet 的 toast 10 秒撤销模式现成可平移：换座/整排交换/切考场/还原） |
| 4 | P3 | 新需求 | 宴会扫码查座：复用 #tpl= hash 编码把座次+速查表打包进链接→二维码，零后端不碰隐私铁律；同时更正 guidesRound7 「需要服务器」的说法 |
| 5 | P3 | 新需求 | /seating 座位级控制：固定某生到某座 + 封座不排人（讲台旁/坏桌/靠门）；可与「按列填充」合并为一个座位操作迭代 |
| 6 | P4 | i18n | 考场筛选选项文案冗余「考场 考场01」/「Exam room Room 1」 |
| 7 | P4 | 文案 | 首页「0.1mm 排版精度」与全站「毫米级 / ≤0.35mm」口径冲突，改回毫米级口径 |
| 8 | P4 | 文案 | 模板 sampleData「考场-1」与「第N考场」混用，统一为第N考场 |
| 9 | P4 | 体验 | Studio 768px 工具栏实测 ~5 行、1024px ~3 行，未达 ≤2 行（顺延项仍在，768 更挤） |
| 10 | P4 | 文案 | 「免费批量生成」系尾 208/225、quickStart note ×52 公式化收束（前轮已记，未收敛） |
| 11 | P4 | 语义 | handoff「班级」列塞图表标题（弱语义混用，可不改，记录在案） |

原始证据：本机 /home/ubuntu/seatmark-r360/（脚本 flow_*.py；截图 f1_* / f2_* / f3_* / f5_* / f6_* / m_* / toolbar_*；页面文本 home.txt / pricing.txt / guides.txt / guide1.txt / seating_empty.txt / banquet_empty.txt / studio_empty.txt / templates.txt）。
