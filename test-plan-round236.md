# 第 236 轮：#239 精确像素大宽度关闭超采样 生产复测

代码依据：#239（commit 92d42fa）pngExport.ts 新增 `exactPixelSupersample(w) = w*2<=4096 ? 2 : 1`，整页与逐张两条导出路径均改用该函数；r234 P2 基线：3840/4000/4096 在旧 bundle `index-EbJxTvBJ.js` 上「正在制作中」>300s 永久卡死。夹具复用 `/home/ubuntu/r231_fixtures/eink234.xlsx`（3 行：张伟234/长名/𫔭𨱏234）。

## T0 部署确认
- 轮询 `https://www.seatmark.cn/studio` entry 直到 ≠ `index-EbJxTvBJ.js`；记录新 bundle 名。未翻转前不做任何判定。

## T1 P2 修复核心：大宽度导出成功
- `/studio?template=eink800` 导入夹具 → 精确像素 + 自定义宽度，逐个 3840/4000/4096 导出。
- 判据：每个在 ≤120s 内出 zip（r234 同环境 >300s 卡死 → 明确区分）；逐张 IHDR = 3840×2304 / 4000×2400 / 4096×2458；纯二值恰 2 色；无 pHYs；文件名含 `-{w}x{h}` 后缀；「正在制作中」遮罩正常消失。

## T2 边界回归：2048（仍 2×）/ 2049 与 3072（1× 直出）/ 3500
- 各导出一次：全部成功、IHDR 精确（2048×1229、2049×1229 或 ×1230=round(2049*120/200)=1229.4→1229、3072×1843、3500×2100）、纯二值无 pHYs。

## T3 预设无回归（像素级对 r234 基线）
- 800×480 与 296×128 预设各导出：IHDR 精确、纯二值、无 pHYs；两者宽 ≤2048 仍走 2× 超采样 → 逐张与 r234 基线 zip（/home/ubuntu/r234_dl/…800x480…0554.zip、…296x128…0554.zip）像素逐位比对，期望逐位一致（若时间戳导致水印/内容差异则如实分析差异像素来源，不默认通过）。

## T4 无效输入拦截回归
- 99 与 4097：输入框红边 +「请输入 100–4096 之间的整数」，点导出无文件产出。

## T5 收尾
- 全程 pageerror=0；清 storage、tabs left: []；写 test-report.md 第 236 轮章节。
