# SeatMark 全流程深度体验对照 R2（生产级把关）

- 环境：本地构建版（vite dev + 进程内 edge API，http://localhost:5173），全程以匿名新用户身份走完流程，未登录（线上邮件未配置，登录链路按约定跳过）。
- 方法：完整走 3 条黄金路径，逐步记录摩擦点/困惑点/文案问题，并与稿定设计、Canva（Bulk Create）同类编辑器逐点对照；所有导出均为真实浏览器 UI 操作产物并经外部工具（pdfinfo / Pillow / unzip）验证。
- 日期：2026-08-06。

---

## 路径 1 — 考场桌贴：Excel 导入 → 字段映射 → PDF 打印

| 步骤 | 观察 | 对照 |
| --- | --- | --- |
| 首页 → 选考场模板进 /studio | 信息密度合理，CTA 清晰，无阻塞 | — |
| 「导入数据」→「下载样例 Excel」 | 立刻下载 `考场座位样例.xlsx` 并有 toast | 优于稿定（场景模板常不给样例表） |
| 上传样例 | toast「Excel 导入成功」，5 条数据，字段全部自动匹配 | 自动映射体验与 Canva Bulk Create 相当 |
| 上传失败场景 | 错误信息为英文原文（FileReader/xlsx 原始 message），中文用户不友好 | 【P2-1，本轮已修复】 |
| 图片版 PDF 导出 | 进度顺畅；pdfinfo 验证 1 页 A4（595.276×841.89 pt） | — |
| 双 PDF 入口 | 「打印 / 矢量 PDF」vs「图片版 PDF（推荐）」对新用户有选择负担；hover title 解释好，但移动端无 hover | 【P2-2，本轮已修复：导出弹窗内加一句话区分】 |
| 无水印配额 | 匿名每日次数耗尽后「今日剩余 0 次」在弹窗最显眼位置，新用户第一反应是被墙 | 【P1-4 增长向，仅记录】Canva 用水印预览 + 价值说明引导登录 |

结论：端到端通过（样例下载 → 导入 → 自动映射 → 真实 PDF 产物验证）。

## 路径 2 — 电子座签：eink 预设 → PNG ZIP（纯黑白）

| 步骤 | 观察 | 对照 |
| --- | --- | --- |
| 模板查找 | 侧栏仅 3 个推荐，需进「浏览全部 216 款」搜索；列表滚动长、分类锚点不够显眼 | 【P2-3，仅记录】稿定模板中心检索路径更浅 |
| 切换模板 | 已导入数据保留、字段自动重映射并 toast | 优于 Canva（换模板常丢字段绑定） |
| 「图片 PNG」对话框 | 默认即精确像素 800×480 + 纯黑白勾选（eink800 模板感知默认值） | 超越 Canva 的细节 |
| 导出 ZIP | 5 张 PNG 全部恰为 800×480、灰度 ⊆ {0,255}、md5 互异（Pillow 断言） | — |
| 导出文件名 | ZIP 与内部前缀恒为「考场座位标签-时间戳」，与电子座签场景不符 | 【P1-2，本轮已修复：前缀跟随模板名】 |

结论：端到端通过，技术断言全部满足。

## 路径 3 — 婚宴席卡：样例下载 → 导入 → 字段命名 ZIP

| 步骤 | 观察 | 对照 |
| --- | --- | --- |
| 「婚庆喜宴」分类 → 婚礼席位卡 → 下载 `婚宴席卡样例.xlsx` | 表头：姓名/桌号/桌名/新人/日期/关系 | — |
| 导入含边界行的改造样例 | 8 条读取（全空行导入端丢弃）；「数据质量提醒：2 行存在已映射字段为空」可高亮缺失 | 数据质量提示是亮点 |
| 字段命名 `{姓名}-{桌号}` | 字段 chip 插入体验好；**直接键入中文出现字符丢失（得到 `{}-{}`）** | 【P1-3 IME/composition 兼容，仅记录，需专项排查】 |
| 说明文案 | 「非法字符自动过滤，重名自动追加 -2，空字段回退序号命名」准确清晰 | — |
| 导出 ZIP 实测文件名 | `唐瑶.png`（桌号空 → 悬挂分隔符折叠）、`8.png`（姓名空 → 前导折叠）、`张伟-1-2.png`（重复 -2）；无任何 `*-.png` 形态 | 本轮第 18 轮回归注记修复，真实导出验证通过 |
| 空行回退 `前缀-序号` | 全空行在导入阶段即被过滤，真实 UI 无法触发该分支，仅单测覆盖 | 覆盖缺口说明，非缺陷 |

结论：端到端通过；文件名清洗修复在真实导出中验证通过。

## 响应式（390 / 768 / 1280，CDP 设备模拟）

- home：三档均无横向溢出。
- studio：**390 宽时最小内容宽度 ~518px**，手机竖屏需横向滚动/缩放；768/1280 正常。【P1-1a，仅记录】Canva 移动端编辑器完全自适应。
- 导出对话框：**390 宽时 scrollWidth 达 800，且对话框半透明与底层面板文字重叠**；768/1280 正常。【P1-1b，仅记录，建议真机复核后专项修复】

## P0 / P1 / P2 清单

| 级别 | 编号 | 问题 | 处置 |
| --- | --- | --- | --- |
| P0 | — | 无（三条黄金路径均可走通） | — |
| P1 | 1a | 390 宽 studio 最小内容宽度 ~518px（移动端可用性） | 仅记录（高风险布局改动） |
| P1 | 1b | 390 宽导出对话框撑到 800px 且半透明叠字 | 仅记录（高风险布局改动） |
| P1 | 2 | 导出文件/ZIP 前缀恒为「考场座位标签」，不随模板场景变化 | **本轮已修复** |
| P1 | 3 | 文件命名输入框直接键入中文丢字符（IME/composition） | 仅记录（需专项排查受控输入） |
| P1 | 4 | 匿名配额耗尽后的登录引导偏弱（增长向） | 仅记录 |
| P2 | 1 | Excel 导入失败错误信息未本地化 | **本轮已修复** |
| P2 | 2 | 双 PDF 导出入口选择负担（移动端无 hover title） | **本轮已修复**（导出弹窗内一句话说明） |
| P2 | 3 | 全部模板列表检索路径较深、分类锚点不显眼 | 仅记录 |

## 本轮修复明细

1. **P1-2 导出前缀跟随模板名**：`PreviewArea.vue` 新增 `exportNamePrefix`（模板名经文件名清洗，空回退「考场座位标签」），PDF/PNG/ZIP 导出统一采用，如 `婚礼席位卡-20260806-1634.zip`。
2. **P2-1 导入错误本地化**：`excel.ts` 文件读取失败/解析失败分别给出中文提示 +「请重新选择文件」引导，不再透出英文原始 message。
3. **P2-2 导出方式一句话说明**：导出选择弹窗顶部按 pendingAction 显示「图片版 PDF」/「打印 / 矢量 PDF」的一句话区分，移动端无 hover 也能看懂。
4. （第 18 轮回归注记）**文件名悬挂/连续分隔符折叠**：`sanitizeFileNamePart` 折叠空字段产生的 `- _ 空格` 混排分隔符并去首尾，整串为空仍回退序号；补 6 组单测，真实导出验证通过。
