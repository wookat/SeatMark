# 站点视觉走查报告 · 2026-08（第四轮）

> 走查对象：第 8 轮新增 UI（`?ref=` 欢迎横幅、导出后分享提示条、配额弹窗、定价页新样式）与本轮（第 10 轮）新增的「导出图片（PNG）」入口与弹窗；
> 视口：1280 / 768 / 390 三档；工具：Chrome CDP + Playwright（截图 + 程序化横向溢出检测 `scrollWidth <= innerWidth`）。
> 对照规范：TDesign / Ant Design（信息条 Alert、弹窗 Dialog、表单项间距与语义色）。

## 一、总体结论

- 走查路由（`/?ref=<8位码>`、`/pricing`、`/studio?template=eink800&demo=1`、PNG 导出弹窗、导出后分享提示条）在 390 / 768 / 1280 三档均 **无横向溢出**（程序化检测全部 `scrollWidth <= innerWidth`）。
- 第 8 轮新增 UI 与既有设计 token（brand/emerald 语义色、圆角 lg、slate 中性灰、内联 SVG 图标）保持一致，未发现阻断性视觉问题。
- 本轮发现 3 项细节问题：2 项低风险已当轮修复，1 项记录待下轮。

## 二、问题清单

| # | 页面 / 位置 | 视口 | 问题 | 级别 | 处理 |
|---|---|---|---|---|---|
| 1 | `?ref=` 欢迎横幅（ShareWelcomeBanner） | 390 | 「一键开始（含演示数据）」CTA 与正文同排挤压，正文被压成窄列（约 40% 宽），阅读节奏差；TDesign Alert 在窄屏惯例是操作区换行右对齐 | P2 | ✅ 已修复：操作区加 `max-sm:basis-full max-sm:justify-end`，窄屏整行换行右对齐 |
| 2 | 导出后分享提示条（PreviewArea） | 390 | 同问题 #1 的挤压模式（`shrink-0` 按钮与 `flex-1` 正文同排） | P2 | ✅ 已修复：同上处理 |
| 3 | 全局 Toast（右上） | 1280 | 上轮问题 #4 仍存在：toast 弹出时覆盖预览工具栏导出按钮区（本轮新增「图片 PNG」按钮同受影响） | P2 | 📋 记录：维持上轮结论（定位策略层面，建议单独评估 toast 移至底部居中），本轮不动 |

## 三、走查对象逐项核对

| 对象 | 核对结论 |
|---|---|
| `?ref=` 欢迎横幅 | brand-50/100 信息语义 + 内联 SVG 礼物图标，与全站语义色一致；`role="region"` + aria-label 可达性齐备；390 挤压问题见 #1（已修复）✅ |
| 导出后分享提示条 | emerald 成功语义正确（成品已拿到场景）；登录/未登录两种文案与 CTA 切换正常；390 见 #2（已修复）✅ |
| 配额弹窗（QuotaLimitDialog） | 复用 ModalDialog（上轮已补 Esc 关闭）；匿名用户配额用尽时导出弹窗内「无水印导出」选项直接置灰并内联提示剩余次数，弹窗仅登录后 429 触发，交互路径一致 ✅ |
| 定价页新样式 | 三档卡片 390 下纵向堆叠无溢出；「Beta 限时免费试用」徽标居中不裁切；语义色（emerald 勾选、brand 主按钮）一致 ✅ |
| 本轮新增「图片 PNG」按钮 + 导出弹窗 | 按钮为内联 SVG 图标（无 emoji）；弹窗内「输出尺寸」分组（SelectField + 宽度输入 + 推导尺寸说明 + 纯黑白复选）层级清晰，与既有水印双选卡片式布局风格一致；390 弹窗内边距与滚动正常，无溢出 ✅ |

## 四、程序化检测结果（横向溢出）

`document.documentElement.scrollWidth <= window.innerWidth`，以下场景 × 3 档视口全部通过：

`/?ref=deadbeef`（含横幅展开）、`/pricing`、`/studio?template=eink800&demo=1`（设置/预览两个 Tab）、PNG 导出弹窗打开态、导出完成后分享提示条展示态。

## 五、PNG 导出功能验证（Playwright + Pillow 图像检查）

- eink800 模板 + 18 人演示名单 → 「图片 PNG」导出，弹窗默认即「精确像素（电子墨水屏 800×480）」+「纯黑白输出」勾选。
- 产物为 zip（18 个 PNG，文件名 `前缀-001.png` … `-018.png`）。
- Pillow 逐张断言：**尺寸精确 800×480**；**像素仅含纯黑 (0,0,0) / 纯白 (255,255,255) 两色**；黑色占比 1.8%–2.5%（非空白）；18 张 MD5 互不相同且肉眼核对含对应姓名/单位/职务（张伟·星汉智能科技、王芳·华建集团等）。
