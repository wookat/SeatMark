# 运营第十六轮记录（2026-08-11）

> 角色：growth/operations（参照 company-os CHARTER 与 SOP-05）。范围：收录复查、IndexNow 全量重推、素材第十六批、人工待办更新。
> 证据目录：`docs/ops-round16-evidence/`。基线：第十五轮 `docs/ops-round15.md`。
> 网络环境说明：本轮出口 IP 为数据中心 IP（54.201.200.193），风控格局与 round15 一致：百度总查询即被滑块验证码拦截，搜狗点选验证码、Google reCAPTCHA 拦截如旧，360 可正常取到页面，Bing 仍兜底污染。均如实记录，未硬闯。

## 一、收录复查（site:www.seatmark.cn，2026-08-11）

| 引擎 | 第十四轮 | 第十五轮 | 本轮实测（2026-08-11） | 变化 |
|---|---|---|---|---|
| 百度 site: | 约 1 个（首页） | 无法核验（滑块验证码） | **无法核验**：总查询即被滑块验证码拦截（「百度安全验证」，`baidu-site-captcha-2026-08-11.png`） | 不可比（沿用 round14「约 1 条仅首页」作基线） |
| Google site: | 无法核验（reCAPTCHA） | 无法核验（reCAPTCHA） | **无法核验**：reCAPTCHA 拦截，IP 54.201.200.193（`google-site-recaptcha-2026-08-11.png`） | 不可比 |
| Bing site: | "About 344"兜底污染 | "About 51"兜底污染 | "About 767 results"，首屏全是 Farm Aid 2016 音乐节等无关结果，**仍为兜底污染，不可作证据**（`bing-site-2026-08-11.png`） | 持平（不可核验） |
| 搜狗 site: | 无法核验（点选验证码） | 无法核验（点选验证码） | **无法核验**：点选验证码拦截（「请依次点击【炼,屁,零,坍】」）（`sogou-site-captcha-2026-08-11.png`） | 不可比（沿用 round11 实测 0 作基线） |
| 360 site: | 0 | 0 | **0**（「抱歉，未找到相关搜索结果」）（`360-site-2026-08-11.png`） | 持平 |

### 新页面收录重点检查（/vs 矩阵 + 两个长尾落地页）

- 本轮唯一可核验引擎为 360：总收录 0，故 **/vs 五页与 /desk-card-generator、/name-card-batch 均未被 360 收录**。
- 百度/搜狗/Google 被验证码拦截，无法核验 /vs 收录变化；百度基线沿用 round14「仅首页约 1 条、/vs 未收录」。
- 结论：/vs 矩阵与长尾落地页第四轮复查在可核验范围内**仍未收录**。数据中心 IP 连续两轮被百度全面拦截，站长平台注册验证（P0）与老板本地网络复查的必要性持续上升。

## 二、IndexNow 全量重推（2026-08-11 00:13 UTC）

- sitemap：`https://www.seatmark.cn/sitemap.xml` 实取 **331 个 `<loc>`**（与 round15 持平），全量推送。
- key 文件 `https://www.seatmark.cn/f04fd03b147f6e5178d97e8e20770a6d.txt` 线上 200，密钥沿用。
- 推送方式：POST JSON（host/key/keyLocation/urlList 331 条）至五端点。

| 端点 | HTTP | 结果 |
|---|---|---|
| api.indexnow.org/indexnow | 200 | 成功 |
| www.bing.com/indexnow | 200 | 成功 |
| yandex.com/indexnow | 202 `{"success":true}` | 成功 |
| search.seznam.cz/indexnow | 200 | 成功 |
| searchadvisor.naver.com/indexnow | 200 | 成功 |

## 三、素材第十六批（已入库 `docs/promo/round16/`，不发布）

- `xiaohongshu-posts-round16.md`：3 篇（导出即所见·会务行政、组合字段多列拼一格·行政、期中考整批课桌贴零坏页·班主任）
- `zhihu-answers-round16.md`：2 篇（预览≠导出成因与三层防线、Excel 多列拼一格的三种做法对比）
- `wechat-article-round16.md`：1 篇（批量打印最贵的是混进坏页——可靠性工程叙事 + 组合字段介绍）

本批统一主打：**「导出即所见、坏页零交付」可靠性叙事**——预览/导出所见即所得（导出引擎逐版本回归验证后锁定，#200/#202）、整页导出逐页截断检测自动重渲、持续截断报错终止绝不静默交付坏产物（#207/#208）、76 篇教程全量质检零死链零事实错误（#204）、300dpi 高清、打印校准 ≤0.35mm、PWA 离线可用、名单不出浏览器；并带**组合字段**（多列拼一格，如「第{班级}·{姓名}」，r199 实测可用）新卖点。所有卖点均有已上线产品事实背书，不含虚构数据。

## 四、人工待办

见 `docs/ops/manual-todo-round16.md`。P0 仍为四大站长平台注册验证（连续七轮未破口）；新增 P0.5 运维项：EdgeOne KV 绑定（账号体系线上不可用的根因）与 SES 发信认证；P6 建议优先发布第十六批素材（可靠性叙事+组合字段）。

## 五、结论与下一步

1. 收录线本轮可核验面与 round15 相同（仅 360 可查，仍为 0）；/vs 矩阵与长尾落地页第四轮复查仍未见收录。站长平台验证提交仍是 P0 唯一破口。
2. IndexNow 331 条五端点全部成功（连续多轮全绿）。
3. 素材第十六批已入库不发布；「导出即所见、坏页零交付」可靠性叙事 + 组合字段是本批主打，发布动作列入人工待办 P6。
4. 下一轮建议：站长平台未验证前收录线只做例行复查；可考虑站内产出「批量打印可靠性」「组合字段用法」教程/落地页，与本批站外素材呼应。
