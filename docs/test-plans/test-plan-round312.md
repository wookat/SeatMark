# 第 312 轮：PR #320+#321 生产复测（/banquet 宴会座位表生成器 + /studio TXT/去重）

前置（已完成）：main=e309d85；生产新 bundle `index-DQa2HNr9.js`；/banquet 返回 200；rev 头 r304（未改边缘函数）。全程匿名。

## T1 /banquet 四步全流程
- 步骤1：粘贴多行「姓名,分组」名单（含 2 条重复姓名）→点「添加到名单（自动去重）」：toast 提示「自动去重 2 条重复姓名」，列表条数=去重后数。FAIL：重复行入列表或无提示。
- 步骤1b：「上传 TXT 名单」传本地 txt（含 1 条与现有重复）：入列去重正确。
- 步骤1c：给分组设自定义颜色（分组色块可改）。
- 步骤2：选一种布局预设（圆桌）生成餐桌；Pointer 拖动一张餐桌位置改变（按住中截图）；改桌名/座位数；添加舞台/入口标记。FAIL：拖动不生效或预设不生成。
- 步骤3：「一键自动分配（同组同桌）」：宾客上桌、同组尽量同桌（同组色聚集）；把一名宾客拖到另一桌微调成功；拖回「未安排」区可撤下。
- 硬判据：localStorage 出现 `seatmark.banquet-state.v1`，刷新后状态保留。

## T2 导出口径（重点回归 #321）
- A4 横向 PNG（带水印）：底边细线 seatmark.cn 水印；**左缘完整不裁切**（放大左边缘核验第一张桌完整）。
- A4 横向 PDF（带水印）：可打开、内容一致。
- **A3 纵向 PDF**：不报错、成功产出（#321 回归判据：此前渲染失败）。
- 勾选「导出带分组颜色」再导出一次：宾客带分组颜色 + 页面含「分组图例」。FAIL：无图例或颜色缺失。

## T3 导出前检查弹窗
- 故意留 ≥1 名未安排宾客 + 添加 1 张空桌 → 点导出：弹窗「导出前检查发现问题」列出「未安排的宾客(N)」「空桌(N)」；点「忽略问题，继续导出」仍可导出。FAIL：不弹窗直接导出。

## T4 移动端 390px
- /banquet 在 390 宽：四步区块可用、无横向溢出（scrollWidth≤innerWidth）、pageerror=0。

## T5 /studio 与 /seating（回归）
- /studio 粘贴名单弹窗：出现「上传 .txt」按钮与「自动去重重复行」开关；粘贴含重复行名单显示「已去重 N 条」；关掉开关重贴则不去重（条数含重复）。
- /seating 页面出现指向 /banquet 的互链；页脚有 /banquet 入口；/seating 原座位表功能预览正常无回归。

硬判据：相关页面 pageerror=0。产出：录屏、报告第 312 轮置顶、导出原始文件存 /home/ubuntu/r312/。
