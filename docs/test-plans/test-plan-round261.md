# 第 261 轮：#265 /studio 粘贴名单导入线上复测（生产，Chromium CDP）

代码依据（4e5a27d）：`excel.ts:196` parsePastedRoster（分列优先级 \t > [,，、] > \s+，`excel.ts:181` HEADER_KEYWORDS 首行判表头，缺列补空 `row[i] ?? ''`，重名表头加序号）；`DataImportPanel.vue:224`「没有文件？粘贴名单」按钮（空数据态与「先用演示数据体验」并排 grid-cols-2）、`:336` ModalDialog size=md + textarea、`:350` 实时「识别到 N 条数据、M 列（…）」、`:360` 空解析禁用「导入名单」、`:181` 成功 toast=「已导入 N 条数据」+（首行已识别为表头 / 未检测到表头，首列已按「姓名」处理）、`:176` 空文本确认→warning「名单为空」。

部署确认：lazy chunk，浏览器打开 /studio 查 DOM 出现「没有文件？粘贴名单」；未出现则每 60s 轮询（≤30 分钟）。本轮姓名标记 张伟261-*（隐私判据）。

## T1 TSV 带表头全链路（核心）
- 点「没有文件？粘贴名单」→ 弹窗出现（标题「粘贴名单导入」）；textarea 填 TSV：`姓名\t班级\t座位号` + 6 行 `张伟261-N\t高三（1）班\t0N`。
- 判据：实时提示逐字含「识别到 6 条数据、3 列（首行为表头：姓名、班级、座位号）」；点「导入名单」→ toast「已导入 6 条数据 首行已识别为表头」+ 面板显示文件名「粘贴的名单」「共 6 条」；预览标签渲染 张伟261-1（截图）；自动映射命中（姓名/座位号出现在标签上）。
- 逐张 PNG 导出：zip 恰 6 张、非空白、md5 互异，落盘 /home/ubuntu/r261_dl/。

## T2 纯姓名列表（无表头分支）
- 粘贴 5 行纯姓名（中间夹 2 空行）。判据：提示「识别到 5 条数据、1 列（未检测到表头，首列将按「姓名」处理）」；确认后 toast 副文=「未检测到表头，首列已按「姓名」处理」、「共 5 条」。

## T3 顿号分隔 + 缺列
- 粘贴 `姓名、座位号` 表头 + 3 行，其中 1 行只有姓名无座位号。判据：识别 3 条 2 列、导入不报错、pageerror=0；打开「查看全部数据」缺列单元格为空。

## T4 空文本/纯空白
- 弹窗 textarea 空：无实时提示行、「导入名单」disabled=true。填纯空白（空格+空行）：仍 disabled（rows=0）。（代码上 disabled 即点不出 warning，warning 分支属防御——以 disabled 为准判据。）

## T5 回归：粘贴导入后重新上传/清空/文件导入
- T1 状态下点「重新上传」（或清空）回到空数据态→「粘贴名单」按钮复现；再用 good40.xlsx 文件导入「共 40 条」正常（Regression，文件链路不受影响）。

## T6 移动端 390px
- 390×844 视口：空数据态两按钮并排可见可点；粘贴弹窗打开无横向溢出（scrollWidth≤390）、textarea 与「导入名单」按钮可达（截图）。

## T7 常规
- pageerror=0；请求标记串（张伟261）命中 0；storage 清理、context 全关、常驻 Chrome 不动。

## 报告
- test-report.md 第 261 轮置顶章节 + 本计划 + #265 复测评论文案 + 如有 SKILL.md 建议。
