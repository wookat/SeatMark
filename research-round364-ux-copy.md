# 第 364 轮调研：用户视角任务流走查 + 去 AI 味专项复核

生产 www.seatmark.cn（部署判据：x-seatmark-rev=r363、首页 bundle=index-CSEolSsL.js、x-seatmark-storage=blob），匿名、未注册未登录，Playwright 连接驻留 Chrome CDP（29229）新建独立 context 走查 + curl + 源码对照实证。测试 context 均为临时 incognito 式 context、用完即关，不落站点存储。**本轮只调研不改代码。**

证据分级：【实证】= 生产环境直接操作/观察；【代码】= 源码定位；【推断】= 未直接验证。

## 一、上轮（r360 ux-copy）发现复核

| 上轮发现 | 本轮结果 |
|---|---|
| P2 /seating 无表头名单首行含「考场01」等词 → 首名学生被当表头 | 【实证】已修。粘贴 `学生NN\t男\t考场01` ×30 无表头 → 「已输入 30 名学生」，无「已跳过表头行」，首行学生保留 |
| P2 考场筛选切换丢已排座次 | 【实证】已修。考场01 随机排座 → 切考场02 → 切回考场01 → 座次原样恢复，toast「已切换到 考场01，已恢复该场上次排座」 |
| P4 考场筛选选项「考场 考场01」冗余 | 【实证】已修。选项现为「考场01 · 15 人」 |
| P4 首页「0.1mm 排版精度」口径冲突 | 【实证】已修。信任条现为「≤0.35mm 校准实测偏差」，与全站口径一致 |
| P4 模板 sampleData「考场-1」与「第N考场」混用 | 【代码】已修。defaultTemplates 等全部改为「第N考场」，「考场-」0 命中 |
| P3 /seating 无撤销 | 【实证】仍缺。点选两座位互换后按 Ctrl+Z 无效（名单保持互换后顺序），全页无撤销按钮；/banquet 的「toast 10 秒撤销」模式（BanquetView 快照撤销）仍现成可平移 |
| P3 /seating 无「按列填充」/座位级控制（固定某生、封座留空） | 【实证】仍缺。填充顺序下拉仍只有「按行填充」「S 形蛇形」两项，无锁定/留空座位 UI |
| P4 定价页维护期 FAQ 仍宣称「注册送 7 天专业版」 | 【实证】部分缓解。文案仍在，但各相关条目均带「账号服务维护中，恢复后可领取」注记；「维护中」字样全页出现 4 处（页首 / 专业版卡 / 两条 FAQ），较上轮重复度下降 |
| P4 seoDescription「免费…批量生成」公式化系尾 | 【代码】仍在且更宽：data 层 seoDescription 含「免费」计 218/231（上轮口径 208/225），「免费批量生成」全文 201 处 |
| P4 quickStart「先看成品效果…名单不出浏览器」 | 【代码】仍在：×53（与上轮持平） |
| P4 handoff「班级」列塞图表标题 | 【实证】仍在。sessionStorage roster 中「班级」字段 = 座位表标题「高三（2）班 期末考试」（弱语义混用，记录在案） |
| Studio 768px 工具栏 ~5 行 / 1024px ~3 行未达 ≤2 行 | 【实证】已修。768px 与 1024px 均约 1 行（分页 + 显示选项 + 打印 + PDF + PNG 收入一行，宽度选项收入「适应…/显示选项」下拉） |

## 二、本轮新发现（按严重度）

### 缺陷 / 体验卡点

1. **【实证·P2】/seating 名单超出座位数时只报数量、不说谁没座。**
   52 名学生 / 48 座 → 仅提示「超出 4 人排不下，请增加行列数」（SeatingView.vue:1013 的 `<span>`，不可点击）；随机排座后座位表 48 席全满，但**页面、座位清单 CSV、导出 PNG 三处都不列出是哪 4 人没座**——实测丢的是「学生02/30/44/46」，老师只能自己拿名单逐个对。真实场景（临时转学、名单多拷一行）下这是刚需信息。**建议**：超出提示可点击展开未排学生名单（并可点名字钉到指定座）；CSV 末尾追加「未排座」段；导出座位图页脚加一行「另有 N 人未排座：…」。

2. **【实证+代码·P3】seating→studio handoff 在「考场号」留空时落到 2 字段留空的模板。**
   演示名单 + 考场号留空 → 一键生成对应桌贴 → 仍用默认「标准考场版」，自动匹配 2/4，每张标签预览显示两个「（空）」（考场、准考证号）。现逻辑（StudioView.vue:218-226）仅在「映射数 < 字段数一半」时才切课桌姓名贴；而内置模板里存在**完全可映射**的「座位号贴」（仅需 座位号+姓名，handoff 名单两列都有，2/2 满配）。**建议**：handoff 后按「未映射字段数最少 / 可全映射优先」挑模板（如座位号贴），或在 toast 里给一键切换入口；考场号已自动按筛选带入，方向对。

3. **【实证·P4】/studio 新手引导卡的「4 步」勾选状态前两步永远打勾，后两步从不打勾。**
   FirstVisitGuide.vue:34-47：`done` 只有 选模板=恒 true、导入名单=hasRows，「核对字段映射与版式」「导出打印」写死 `done: false`——导出完成后卡片是靠 dismissStudioGuide 整体消失，若用户关掉卡片再手动…实际观感：走完全流程后（未关卡片）第 3/4 步仍是空心圈，进度反馈不实。**建议**：第 3 步 done=mapping 无未映射或已人工确认、第 4 步 done=导出过一次（导出成功本来就会 dismiss，矛盾点就在「导出即消失」与「打勾进度的设计意图」不一致，二者留一即可）。

### 新需求候选

4. **【推断·P3】宴会「扫码查座」（r360 已提，本轮确认缺口仍在且站内叙事仍写死为「不可能」）。**
   guidesRound7 仍写「这需要把名单放到某个服务器上…与名单不上传冲突」。但站内已有零后端先例：`#tpl=` hash 编码分享模板（share.ts，全程无请求）。同样可把「排好的座次图 + 姓名→桌号索引」deflate+base64url 进链接生成二维码，宾客手机打开本地渲染只读查座页，名单不出链接。中小型宴会名单 deflate 后体积在 URL/二维码容量内。属产品新需求，非缺陷；若不采纳，建议把教程里的理由从「做不到」改为「暂未提供」。
5. **【推断·P4】宴会「必须同桌」配对。** 现有「不同桌」排斥对 + 分组归并可近似（新建两人小组），但婚礼策划常见诉求是「双方父母同桌」「新人+证婚人同桌」，让用户为每对建一个分组比较绕。可在排斥对旁边加「同桌对」清单，自动分配时优先同组之外再合并同桌对。
6. **【实证·P4】/banquet 宾客速查表只有「调起浏览器打印」一条路。** 说明文案已教「另存为 PDF」，但桌面端打印对话框里选 PDF 是隐性操作、移动端尤其不便；婚礼策划常要把速查表 PDF 发给酒店/签到台人员。建议给一个「下载速查表 PDF」按钮（复用图片版 PDF 的渲染路径）。

### i18n / 措辞残留（均 P4）

7. **【实证】同一界面双名并存**：导航 CTA/页脚/多数文案叫「标签工坊」，但 Studio sr-only H1 与宴会页 handoff 文案（BanquetView.vue:1818-1819）、guidesRound7 叫「座签工坊」——「座签工坊」全站 ×12、「标签工坊」×122。建议统一为「标签工坊」（或反向统一），用户查帮助/反馈时不会对不上名字。
8. **【实证】定价页「维护中」提示虽去重后仍有 4 处同义重复**（页首横幅 / 专业版卡 / 两条 FAQ）。每处语境不同可接受，但可考虑 FAQ 条目只在有账号相关动作的条目下保留注记。
9. **【代码】公式化收束未收敛**：seoDescription「免费…生成」系尾 218/231、quickStart note ×53、「名单不出浏览器」×73 —— 前轮已记，仍建议按场景微调配词。

## 三、去 AI 味专项复核（本轮新挖面：商业黑话/行业腔词表 ×110+、模板详情通读、速查表/toast 抽样）

三道禁词护栏（bannedCopy.spec.ts，含「拉满/效率翻倍/抓手/链路/利器/大幅提升」等）持续有效，全量扫描站内文案**基本干净**，AI 腔属个别残留：

1. **120+ 营销/AI 腔词表扫描结果**：神器/天花板/宝藏/安利/绝绝子/敲黑板/重磅/官宣/爆款/ins风/松弛感/降维打击/吊打/秒杀/教科书级/殿堂级/有手就行/秒变/黑科技/诚意满满/倾力打造/旗舰/顶配/一劳永逸/独家首发/震撼来袭/敬请期待/赋能/焕新（仅 1 处正当，见下）/颠覆/革命性/划时代/完美解决/轻松搞定/一键搞定/傻瓜式/小白也能/分分钟/告别繁琐/解放双手 等 —— 全部 0 命中或仅出现在测试文件与代码注释（不构成用户可见文案）。
2. **边缘命中（建议白描化，均 P4）**：
   - `guidesRound4.ts:873`「自打自贴几乎是**零门槛**」→「几乎不用花钱，也不用等快递」
   - `guidesRound4.ts:547`「花一下午**精雕细琢**模板」→「花一下午慢慢调模板」
   - `guidesRound5.ts:131`「推荐**组合拳**：」→「推荐这样配套做：」
   - `templateDetailsRound2.ts:1947`「物业一次就能**焕新**」→「物业一次就能全换完」
   - `templateDetailsRound4.ts:946`「深空底更暗黑**硬核**」→「深空底更暗、更压得住场」
   - `guides.ts:564`「可以**沉淀**邮件合并模板」→「可以长期复用现成的邮件合并模板」（其余「沉淀/复盘/对标」命中均为正常中文或代码注释，可不改）
   - `guidesRound2.ts:1132`「经验就**沉淀**成了制度」——口语里已通用，可保留，记录在案。
3. **称呼一致性**：全站「你」体；演示物料里「感谢您的耐心等候」属请柬固定语，正当。
4. **EN 侧**：35+ 营销词（seamless/elevate/unlock/hassle-free/look no further/game-changer/moreover/cutting-edge/supercharge/effortless/robust 等）0 命中；/en 七条路由可见 CJK 仅剩语言切换「中文」一处——模板名、描述、演示数据（Alex Chen / Class 9-5）全部英文化，且 /en/templates 主动声明「tutorials are in Chinese only」并给中文入口，诚实且体面。
5. **教程正文抽样通读**（婚宴机动桌/找座篇）：结构保持「先说结论 + 具体步骤 + 诚实缺口 + FAQ」，「暂无扫码查座，下文不会把它写成已有功能」这类声明是反 AI 腔的正面例子。
6. **导出对话框**（PNG/图片版 PDF/矢量 PDF 三处）：「本次输出：会议大桌牌 · A4 纵向 · 18 张 PNG…」「页脚一行 seatmark.cn 细线签名」等均为具体、可验证的白描；r306 遗留的「半透明徽章式品牌水印」旧口径已清除，与 #314 细线签名实现一致。
7. **公式化收束**：见二.9，仍是「功能可信但连读显拼接感」的老项。

## 四、正向确认（本轮实证通过）

- 三 persona 黄金链路全通，全程 pageerror=0：
  - **班主任**：/seating 粘贴名单（含表头/无表头/含考场列/含性别列）→ 考场筛选与分场排座保留 → 点选换座 → 座位清单 CSV → A4 横向 PNG（3509×2481，讲台/过道/座位号齐全，1s 内）→ 一键生成对应桌贴进 /studio；
  - **婚礼策划**：/banquet 演示名单 48 人 3 组 → 一键自动分配（48/48，拆分 3 组逐条列明并给「换用优先坐满」入口）→ 不同桌面板 → 导出前检查弹窗如实列空桌并给三选一 → PDF 导出 1.6s（张贴版六桌铺满，水印为底边细线签名）；
  - **行政**：/studio 导入含「序号/姓名/公司/单位/职务/桌号/联系电话/备注」的会议名单 → 自动映射 2/2、明示「未使用列：序号、职务、桌号、联系电话、备注」→ 会议大桌牌 PNG zip 3.5s 出件，单张覆写小技巧可见。
- 移动端 390px：/、/seating、/banquet、/studio、/templates、/pricing 均无文档级横溢。
- /seating 溢出提示、上传名单文件（.txt/.csv/.xlsx/.xls）、拼音搜索、「排不下」提示均工作。
- 新手引导关闭后写入 localStorage 并不再出现（此前疑似「关不掉」实为误点其他 ×）。
- 降级态诚实：/api/auth/me 503 期间定价页/配额处明示「账号服务维护中」而非装死。
- 测试后各临时 context 已关闭，无站点存储残留。

## 五、排期建议清单

| # | 严重度 | 类型 | 内容 |
|---|---|---|---|
| 1 | P2 | 缺陷 | /seating 人数超座位时只说「超出 N 人」，不说谁没座：建议提示可展开名单、CSV 追加未排段、导出图页脚注明「另有 N 人未排座」 |
| 2 | P3 | 缺陷 | seating→studio handoff 在考场号留空时落到 2 字段留空的默认模板；建议按「可全映射优先」挑模板（如座位号贴 2/2），或 toast 给一键切换 |
| 3 | P3 | 缺陷 | /seating 无撤销（换座/整排交换误操作只能整体还原；Ctrl+Z 实证无效；/banquet 的 toast 撤销模式可平移） |
| 4 | P3 | 新需求 | 宴会扫码查座：#tpl= hash 编码座次+速查索引→二维码，零后端；如不采纳，把教程里「与名单不上传冲突」的理由改为「暂未提供」 |
| 5 | P3 | 新需求 | /seating 座位级控制：按列填充 + 固定某生到某座 + 封座留空（前轮已记，仍缺） |
| 6 | P4 | 新需求 | 宴会「必须同桌」配对（婚礼双方父母等）；现状需建两人分组绕行 |
| 7 | P4 | 缺陷 | /banquet 宾客速查表仅浏览器打印路径，无 PDF 文件下载（移动端「另存 PDF」不便） |
| 8 | P4 | 体验 | 新手引导卡第 3/4 步 `done` 恒为 false，进度勾选不实；与「导出即消失」逻辑二选一 |
| 9 | P4 | 文案 | 「座签工坊」/「标签工坊」双名并存（12 vs 122），建议统一 |
| 10 | P4 | 文案 | 去 AI 味边缘命中 6 处白描化建议（零门槛/精雕细琢/组合拳/焕新/硬核/沉淀，见三.2） |
| 11 | P4 | 文案 | seoDescription「免费…生成」系尾 218/231、quickStart note ×53、handoff「班级」塞标题（前轮已记，未收敛/未改） |
| 12 | P4 | 文案 | 定价页维护期「注册送 7 天」宣称仍在（已有维护注记）；「维护中」同义提示全页 4 处 |

原始证据：本机 /home/ubuntu/seatmark-r364/（脚本 s1_-s28*.py；截图 home_*、seat_*、banquet_*、meeting_*、dlg_*、m_*、en_*、tb_*；导出物 seating.png、seating.csv、seat0.csv、seat1.csv、overflow.csv、banquet.pdf、quickref.bin、pertable.csv、meeting.xlsx、meeting_export.zip、meeting_png/、multi.xlsx）。
