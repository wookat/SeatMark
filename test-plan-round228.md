# 第 228 轮：Excel 导入字段映射边界形态走查（生产，无代码变更轮）

代码依据：excel.ts:89-109（空表头补名 `列N`；headers 重名时 `record[header]` 后列覆盖前列——重复列名疑似静默丢前列，重点取证）；excel.ts:61（raw:false 读格式化文本 cell.w，公式应读计算结果）；excel.ts:85-87（<表头+1 行数据 → throw「Excel 至少需要包含表头行和一行数据」）；workspace.ts:564 importExcel 的 toast 错误透出；MappingPanel.vue:20-28（映射下拉 = headers 原文）。
夹具现场 python3+openpyxl 生成于 /home/ubuntu/r228_fixtures/。每形态导入后检查：映射下拉选项、数据表「共 N 条」、预览首卡字段值。

## T1 重复列名（两列「姓名」，值不同：A列=甲X、B列=乙X）
- 导入后 headers 下拉如何呈现（两个同名项?）；预览/数据取到的「姓名」值=甲X 还是 乙X;若 A 列值不可达且无任何提示 → **静默丢列，定级**；如实记录。

## T2 空表头列（表头 [姓名,'',备注]，空列下有数据）
- 下拉出现「列2」选项且可映射；映射到列2后预览显示该列值 — passed 判据：列2 数据可达非丢弃。

## T3 公式列（C2==A2&B2）
- 导入后该列值为计算结果串（如 张三1班），非 `=A2&B2` — passed。

## T4 宽表 100 列×20 行
- 导入完成（共 20 条）、无卡死（导入耗时取证 <15s）、映射下拉恰 100+2 项可打开、pageerror=0；截图。

## T5 超长表头（200 字符列名）
- 下拉与数据表 UI 不破版（无横向溢出把布局撑爆，body scrollWidth 不超视口+容差；截图人工核验）；该列可映射且值正确落卡。

## T6 混合类型列（同列：42 / 文本 / 2024-01-05 / 007 / 50%）
- 预览/数据表按 Excel 所见文本呈现（前导零 007 保留、百分比/日期为格式化文本），取证实际渲染串如实记录。

## T7 只有表头无数据 + 1×1 最小表
- 两者均 toast/错误透出「Excel 至少需要包含表头行和一行数据」（或等价明确提示），数据区不出现空导入假成功 — passed。

## T8 收尾
- 全程 pageerror=0；清 storage 关全部 tab；第 228 轮置顶追加 test-report.md。发现 P 级即时 message_parent。
