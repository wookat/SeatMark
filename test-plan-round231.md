# 第 231 轮：照片链路专项（生产，无代码变更轮）

代码依据：photos.ts:51-97（匹配：去扩展名后完全一致优先→包含匹配取最长值（≥2 字符）；完全一致命中后不被包含命中覆盖；未匹配/非图片进 errors）；workspace.ts:639-656 importPhotos（未选匹配列 toast「请先选择匹配列」；成功 toast「照片已加载 本次匹配 N 张，当前覆盖率 X%」）、:505-523 applyExcel→clearPhotos（每次导入/切 sheet 清照片）、:153-155（刷新后照片仅内存、toast「照片需重新上传」）；MappingPanel.vue:209-249（照片匹配区：匹配列 select、上传按钮、覆盖率行「已导入 N 张照片，匹配 m/M 行（覆盖率 X%）」、errors details）；模板「照片核验版」（defaultTemplates.ts:567）含列匹配 photo 字段。

夹具：名单 3 行（姓名 张伟/李娜/王强，学号 20230101/20230102/20230103）；纯色 JPEG/PNG（红/绿/蓝等，可做像素断言）。

## T1 真实照片导入与覆盖率
- 切模板「照片核验版」→ 导入名单 → 匹配列=姓名 → 上传 张伟.jpg（红）+ 李娜.png（绿）：toast「照片已加载 本次匹配 2 张，当前覆盖率 66%」（2/3）；覆盖率行文本一致；预览卡片中张伟卡出现红色照片、王强卡为占位（截图像素级确认红/绿色块存在）。
- 未选匹配列直接点上传按钮应 disabled（MappingPanel:226）。

## T2 带照片状态下重置的 UI 级实证（r230 缺口）
- 带照片状态下重导同一名单文件：photos 清空——卡片照片消失、覆盖率行消失（totalPhotos=0 不渲染）、无「照片已清除」类提示（如实记录有无提示）；截图前后对比。
- 切 sheet / 换文件 B 同理抽 1 项验证。
- 刷新页面：名单恢复 + toast「照片需重新上传」出现（workspace.ts:155）。

## T3 匹配边界（一次多文件上传取证 errors/覆盖计数）
- 完全一致优先：先传 张伟20230101.jpg（蓝，包含命中张伟）再传 张伟.jpg（红，完全一致）→ 张伟卡最终为红；反序（红先、蓝后）→ 仍红（exact 不被包含覆盖）。
- 大小写扩展名 李娜.JPG 命中；文件名含空格/括号「王强 (1).jpg」→ 包含命中王强；
- 一文件含两值（张伟20230101）在匹配列=学号 时命中学号行；
- 未匹配文件 无名氏.jpg → errors details「无名氏.jpg - 未找到匹配的数据行…」且 unmatched 计数；伪图片 张伟.jpg（文本内容）→「不是有效的图片文件」。

## T4 照片行导出像素断言
- 覆盖态下 PNG 带水印导出 → 解包 zip → 用 PIL 检查张伟标签图中存在明显红色像素区（非空位/裂图）。

## T5 隐私
- 全程 Network 捕获：请求 URL/postData 中照片文件名（张伟/李娜/王强/20230101）与 base64 图片体命中 0；无上传类 POST 带二进制。

## T6 收尾
- 全程 pageerror=0；清 storage 关全部 tab；写 test-report.md 第 231 轮章节。P1/P2 即时上报。
