# 第 344 轮调研：真实用户任务流走查 + 去 AI 味文案专项

生产 www.seatmark.cn（部署判据：x-seatmark-rev=r343、index-BOMQmRwx.js、main=77ca186），匿名、未注册未登录，CDP 驱动驻留 Chrome（29229）+ curl 实证，测试后已清空浏览器存储。本轮**只调研不改代码**。证据分级：【实证】= 生产环境直接操作/观察；【代码】= 源码定位；【推断】= 未直接验证的判断。

## 一、三个人物的任务流实走

### A. 班主任排考场（/seating）

走查路径：粘贴「姓名 性别」名单 → 男女混排 → 打印座位表 → 一键生成桌贴接力。

- 【实证】粘贴 24 人名单（空格分隔姓名+性别）→「已输入 24 人 / 48 座」，男女混排后预览入座正常；A4 横版打印产出正确（讲台、6×8 网格、姓名入格、底边签名水印）；「一键生成对应桌贴」跳 `/studio?from=seating`，名单携 座位号/排/列/班级 四列完整落地（sessionStorage 已核对），自动切到桌贴类模板。
- 【实证·P1】**Excel 复制的多列文本直接把名单打爆**：粘贴三列 Tab 分隔（姓名\t性别\t学号）3 行 →「已输入 9 人」，「男」「20260101」等单元格各变成一个学生；粘贴带表头的两列（姓名 性别\n张三 男…）→ 6 人，「姓名」「性别」被当成学生。textarea 占位符只声明「空格/逗号分隔」，而从 Excel 复制出来的是 **Tab**——班主任最自然的动作恰好得到错乱名单，且全程无任何提示。对照：/studio 的「粘贴名单」弹窗已有首行表头识别 + 多列拆分 + 「识别到 N 条 M 列」实时提示（r263/#267），/seating 未复用同一解析器。
- 【实证·P3】名单无性别列时点「男女混排」静默无反应——无 toast、无提示（静态说明文字有，但点击瞬间零反馈），用户无法判断是没生效还是已经混排。
- 【实证】座位填充顺序有 按行填充 / S 形蛇形 两档；无「固定座位」能力——想把某生固定在讲台旁，只能事后逐个交换，随机/混排会再洗走。

### B. 婚礼策划排桌（/banquet）

- 【实证·P1】**宾客名单无法带分组导入**：粘贴「张伟，男方亲友」式两列 10 行 → 名单（13 人），「男方亲友」「女方亲友」「同事」被自动去重后变成 3 个假宾客，与真人并列待安排；上传按钮只接受 `.txt`（`input[accept=".txt,text/plain"]` 实证），不收 .xlsx/.csv——而真实婚宴名单几乎都是 Excel。导入后分组只能**逐位**宾客点开「未分组」下拉指派（每客一个 listbox，实证），无多选/批量分组：150 人婚礼 = 150 次下拉。
- 【实证】自动分配、空桌前置检查（「空桌 6 桌：3号桌…」+ 三选出口）、拖拽微调、删除空桌均正常；390px 移动端无文档级横向溢出，有「画布超宽时可左右滑动查看」提示。
- 【推断·P2 新需求】无「指定桌 / 必同桌 / 不同桌」约束：主桌指定席位、离异父母或旧怨分桌这类婚宴刚需，目前只能自动分配后逐人拖。

### C. 行政做会议桌牌（/studio）

- 【实证】`?template=meetingTent` 深链 + 上传 8 行 xlsx（姓名/部门/职务/座位号）→「共 8 条」，姓名自动映射，「单位」未映射在导出前置提示中如实列出（「1 个字段未映射（单位），成品中将留空」+ 去映射/仍然导出）；模板选择器中文搜索「会议桌牌」可命中（r306 记录的中文搜索疑点本轮已不复现，保险起见建议回归用例里保留）。
- 【实证】PNG 导出面板的「带水印」描述已更新为「细线签名式」（r306 遗留的「徽章式」旧文案已修）。

### 横截：可发现性

- 【实证·P2】首页首屏全部 CTA 只指向 /studio；`/seating`、`/banquet` 两个独立工具在整页 `<a>` 中各只出现 1 次（页脚）。顶部导航（首页/模板/教程/定价）无排座入口，hero「快捷入口」5 个全是 SEO 落地页。班主任、婚礼策划两个人物从首页自然路径都会先进 /studio（做标签），而不是排座位——三大工具的可发现性不对称。

## 二、去 AI 味专项（站内文案审查）

### 结论先行

中文主站文案**总体已达标**：全库检索 赋能/一站式/轻松搞定/神器/极致/告别/解放双手 等套话零命中；教程正文（抽样 exam-seat-label-batch-print 全文）是具体人话（含「160g 哑光卡纸」「打印缩放设实际大小」这类实操细节）；225 款模板描述统一「N 列 × M 行 + 场景 + 风格」信息句式，模板详情页 intro 是带画面的具体描写（「深空蓝黑打底，两道青到紫的斜切光带」）。空洞套话、排比堆砌问题**未发现系统性残留**。

### 发现的文案/用词问题（按严重度）

1. 【实证·P2 英文界面共享词条串台】`'人' → 'guests'` 是全局词条，串进教室场景：/en/seating 显示 "Entered 24 guests / 48 seats"（班级名单应是 students/名）。同根问题：模板里硬编码全角标点泄漏到英文 UI——`SeatingView.vue:552` `{{ tr('座') }}。` → "48 seats。"；`PricingView.vue:218` `（<RouterLink>去兑换</RouterLink>）；` → /en/pricing 实证渲染 "redeem codes（Redeem now）；"；`BanquetView.vue:1311` `{{ tr('桌') }}：{{ issues.emptyTables.join('、') }}` → EN 版导出前检查会出 "6 tables：3号桌、4号桌"（全角冒号+顿号连接）；同类 `（{{…}}）` 模式散见 BanquetView:1306/1318/1328、AccountView:574/667、StudioView:296-298、DataImportPanel:382、LayoutPanel:362。**这是 i18n 结构性问题，不是逐条错字**——建议全角标点入词条或按 locale 渲染，并给单字词条（人/座/桌/排/列）加语境。
2. 【实证·P3 英文措辞】'排'→'Row'（大写单数）与 '列'→'cols'（小写复数）混用 → "6 Row × 8 cols"；/en/pricing "free sign-in raises it"（注册才是 sign up）、"reservable" 生硬（建议 "Reserve now / Reservation opens"）。
3. 【实证·P3 数字口径不一致】首页 FAQ 写「150+ 款内置模板」（HomeView.vue:215，en.ts:85 同步），同页 stat 区与 /templates 均为「225 款」，/vs 对比页写「222+ 款」（vsPages.ts:209）——三个数字同时在线，150+ 明显是旧值未更新。
4. 【实证·P4 风格备注】页面 `<title>` 为 SEO 关键词堆叠（"座签·桌牌席卡·门贴证卡批量生成 - SeatMark 座签 | Excel 批量打印"），浏览器标签/分享卡片机读感强；首页副标「粘进来 → 看预览 → 导出」箭头流水线 + 「需要准备 / 最后得到」对照句式略模板腔——在可接受范围，仅记录不改也可。

### 英文教程区

【实证】/en/guides 有明确的中文-only 声明（"This section is currently available in Chinese only"）并将 76 篇教程标题直排中文展示，诚实但不友好；声明称 "Studio, Seating, Banquet, Pricing pages are fully in English"——与上面第 1 条的全角标点泄漏略有出入。

## 三、需求/卡点清单（给排期用）

| # | 严重度 | 类型 | 内容 |
|---|---|---|---|
| 1 | P1 | 缺陷 | /seating 粘贴不识别 Excel Tab 多列 + 不识别表头行，错名单零提示 |
| 2 | P1 | 缺陷+缺口 | /banquet 不支持「姓名,分组」两列导入、不收 xlsx/csv、无批量分组 |
| 3 | P2 | 可发现性 | /seating、/banquet 首页仅页脚各 1 链，两大目标人物难以触达 |
| 4 | P2 | i18n | 全角标点硬编码出框 + '人'→guests 串台（教室场景应为 students） |
| 5 | P2 | 新需求 | 宴会「指定桌/必同桌/不同桌」约束；教室「固定座位」 |
| 6 | P3 | 反馈缺失 | 无性别列点「男女混排」静默无提示 |
| 7 | P3 | 文案 | 模板数 150+/222+/225 三个口径并存；EN 措辞小修 |
| 8 | P4 | 风格 | title 关键词堆叠、hero 句式模板腔（可不改） |

## 四、正向确认（本轮实证通过，非问题）

- /seating 全链路：粘贴→混排→A4 横版打印（PDF 841.92×594.96pt 实证）→一键带名单进 /studio。
- /banquet：自动分配、导出前检查（空桌/未安排/超员预检）、删除空桌、移动端布局。
- /studio：xlsx 导入→自动映射→未映射前置提示→导出面板的细线签名水印描述已与实物一致。
- /en 四大页（home/pricing/seating/banquet/studio）静态与渲染态均基本英文化，残留中文仅 中文 切换钮/宋体/湘ICP备/模板缩略示例，属正当。

原始证据：本机 /home/ubuntu/r344/（截图 seat_*.png、banq_*.png、studio_*.png、seating_print.pdf、各页文本 dump）。
