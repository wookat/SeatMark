# 第 263 轮：#267 粘贴名单「首行是表头」手动开关线上复测（生产，Chromium CDP）

代码依据（9292c53）：`excel.ts:197` `parsePastedRoster(text, firstRowHeader?)`，`:214` `firstRowHeader ?? 关键词启发式`——显式传入覆盖启发式；`DataImportPanel.vue:166` pasteHeaderOverride（null=自动）、`:172` togglePasteHeader、`:178` 打开弹窗重置 null、`:366-372` CheckboxField「首行是表头」`:model-value="pasteParsed.headerDetected"`（勾选态如实反映当前解析结果）。r261 P4 复现夹具：首行含关键词「手机」的纯数据行。

部署确认（T0）：/studio 打开粘贴弹窗、textarea 输入任意文本后 DOM 出现「首行是表头」复选框；未出现则每 60s 轮询（≤30 分钟）。姓名标记 张伟263/王芳263。

## T1 P4 修复主判据（误判→手动纠正）
- 粘贴 `张伟263手机甲\n张伟263手机乙`。
- 判据：默认提示「识别到 **1** 条数据、1 列（首行为表头：张伟263手机甲）」且复选框 checked=true（截图）；**取消勾选**后提示变「识别到 **2** 条数据、1 列（未检测到表头，首列将按「姓名」处理）」且 checked=false（截图）；点导入 → toast「已导入 2 条数据 未检测到表头…」+「共 2 条」+ 预览含 张伟263手机甲（首条不再丢失）。

## T2 反向（自动不判→手动指定为表头）
- 清空后重开弹窗，粘贴 `张伟263甲\t男\n王芳263\t女`（无关键词）。
- 判据：默认 checked=false、提示「识别到 2 条数据、2 列（未检测到…）」；**勾选**后提示「识别到 **1** 条数据、2 列（首行为表头：张伟263甲、男）」（表头如实取首行）且 checked=true；导入 → toast「已导入 1 条数据 首行已识别为表头」、数据查看器仅 1 行 王芳263。

## T3 重开弹窗重置为自动
- T2 手动勾选导入后清空、重开弹窗，粘贴 T1 同文本（含「手机」）。
- 判据：提示回到自动误判态「识别到 1 条数据…首行为表头」、checked=true（前次手动 false 不残留——若残留会显示 2 条，可区分）。

## T4 回归（Regression）：TSV 带表头正常链路
- 粘贴 `姓名\t座位号` + 4 行 张伟263-N。判据：自动识别「识别到 4 条数据、2 列（首行为表头：姓名、座位号）」、checked=true 未动手；导入「共 4 条」；逐张 PNG 导出 zip 恰 4 张 1000×534、md5 互异、0 空白，落盘 /home/ubuntu/r263_dl/。

## T5 移动端 390px
- 390×844 弹窗粘贴 T1 文本：提示行+复选框可见不破版（scrollWidth≤390、复选框 bounding box 在视口内、截图）；取消勾选 → 2 条，导入成功。

## T6 常规
- pageerror=0；请求标记串（张伟263/王芳263）命中 0；storage 清理、context 全关、常驻 Chrome 不动。

## 报告
- test-report.md 第 263 轮置顶章节 + 本计划 + #267 复测评论文案 + 如有 SKILL.md 建议。
