# 第 369 轮调研：用户视角走查 + 去AI味专项（只调研不改码）

> 范围：以三种真实用户身份（班主任排考场、婚礼策划排桌、行政做会议桌牌）在 https://www.seatmark.cn 匿名完整走任务流；复核第 365–368 轮遗留发现；专项审查站内文案/教程/模板演示文字的 AI 腔残留。
> 证据标注：【亲测】= 本轮用 Playwright 连驻留 Chrome（CDP 29229）在真实生产站操作/下载/读 localStorage 验证；【代码】= 仓库源码定位；【推断】= 由证据推导。
> 铁律遵守：全程匿名，未注册、未登录、未提交反馈/预订表单、未发送真实邮件；走查在隔离 browser context 中进行，context 关闭即清理本地存储；只实测带水印免费导出与免费下载型导出，未消耗无水印额度。
> 生产版本：走查时主包 index-O_pGoJWQ.js（r368 前端部署）；X-SeatMark-Rev 仍为 r367（edge-functions 本轮无变更，符合预期）。/api/auth/me GET 匿名实测返回正常响应（x-seatmark-storage: blob），r368 记录的 503 未复现。

## 一、本轮最重要的新发现

### 【P1｜亲测】/banquet 空格分隔「姓名 分组」粘贴被静默错解析——与页面示例自相矛盾

- 宾客名单输入框上方提示原文：「第二列可填分组，自动归组；支持从 Excel 直接复制『姓名、分组』两列，如：张伟 男方亲友」——示例用的就是**空格分隔**。
- 实测（localStorage `seatmark.banquet-state.v1` 读回验证）：
  - 粘贴 `张一 男方亲友\n李二 女方亲友\n王三 同事`（空格两列）→ 生成 **6 位宾客**：`张一`、`男方亲友`、`李二`、`女方亲友`、`王三`、`同事`，全部 `groupId: null`。分组名被当成宾客导入，且无任何提示。
  - 真实场景放大版：110 行「姓名 分组」粘贴后名单变 115 人（110 姓名 + 5 个去重后的分组名当上了宾客），自动分配后「男方亲友」这个人被实实在在安排了座位。
  - 三词行 `张九 男方亲友 A` → 整行被当成**一个**带空格的宾客名导入（另一套分支）。
  - Tab 分隔（真从 Excel 复制）与逗号、顿号分隔 → 正常弹出「确认名单解析方式」三选弹窗（按列分组 / 仅第一列 / 全部按姓名），该弹窗本身设计良好。
- 结论：触发解析确认弹窗的分隔符集合不含空格，而示例恰恰以空格书写。真实用户从微信/备忘录/手打名单过来几乎都是空格分隔——这是一个「照官方示例操作必然得到脏数据」的入口级缺陷。
- 修复方向（供参考）：① 每行恒定为「姓名 + 同一分隔符 + 单个尾词」时同样触发解析确认弹窗；② 或把提示示例改成真实可识别的分隔写法（如「张伟⇥男方亲友（Excel 复制）或 张伟，男方亲友」），并说明空格分词用于一行多人；③ 附带收益：空格两列场景下「男/女方亲友」等词高频重复，可做兜底识别。
- 注：`Mary Smith` 这类英文空格名按整行单人导入（符合预期），所以规则不能简单改成「空格即分列」——这也是为什么要走确认弹窗而非静默按列。

### 【P2｜亲测+代码】/seating 演示名单性别与姓名错配（r368 复测结转，生产仍在）

- 生产实测：点「用演示名单」载入 48 人，名单域直接渲染 `李娜 男`、`陈静 男`、`王伟丽 男`、`胡华艳 男`、`邓杰霞 男`、`何发芳 男` 等——肉眼可辨的女性名字标男；男女混排后座位格按该错误性别显示男/女底色（`.seating-seat--boy/--girl` 实测交替着色，「李娜」染男色）。
- 【代码】`SeatingView.vue:478-479`：`demoPersonNames(seatCount, locale).map((name, i) => `${name}\t${genders[i % 2]}`)` ——未使用 `demoDatasets.ts` 已有的 `NAME_GENDERS`/`demoGenderOf`（前 24 个池内名字本有正确性别，也被奇偶覆盖错）。`excel.ts:335` makeDemoRows 性别列同为 `i % 2`（当前仅首屏演示引用、性别列不外显，低影响，同 r368 记录）。
- 影响：「男女混排」是本页核心卖点，演示数据即暴露错配，削弱可信度；顺带合成名（孙娜玉、梁芳涛、彭平磊、许兰武等随机双字）读起来也偏机器拼合，属「演示数据 AI 味」。

### 【P2｜亲测】/seating 缺「尽量散开/自适应间距」填充——间隔三档全部容量减半

- 现有「座位间隔」= 不隔 / 隔位（每排隔一列）/ 隔排 / 棋盘，6×8 教室后三档可坐数全部 48→24。
- 实测场景：30 人名单（小班考试塞进 48 座教室——拉开间距防作弊是真实诉求）→ 选任一档间隔都报「超出 6 人排不下，请增加行列数」，6 人列入「另有 N 人未排座」脚注。提示与未排座清单本身做得好，但**没有任何一档能让 30 人全员入座且尽量散开**——要满足只能放弃间隔回到不隔（全堆前 4 排）或手动逐个拖。
- 新需求：加一档「尽量散开」（按人数自适应：行优先隔列、放不下再放开列位/启用隔行组合，目标=全员入座前提下人均间距最大）；或在提示里指路「24 座坐不下 30 人：可改棋盘/不隔」式的模式级建议。

### 【P3｜亲测】/banquet 零安排时可直接导出全「待安排」CSV/速查表，无预检拦截

- 实测：导入名单、未做任何分配时点「按桌名单 .csv」→ 直接下载，全部行 `桌名=待安排`；「宾客速查表 下载 PDF」同样直接出（jsPDF 生成的空表）。PNG/PDF 主导出通道有「导出前检查」预检（未安排 N/空桌 N 列出），CSV 与速查表不在预检范围。
- 建议：CSV/速查表导出时若 `未安排 > 0` 弹同一个预检（或至少 toast 提示「35 位宾客尚未安排，导出名单位将标记待安排」）。

### 【P3｜亲测+代码】/seating→/studio 桌贴 handoff 丢弃附属列（学号等）

- 实测：排座名单粘贴「姓名 性别 学号」三列 → 提示「已忽略 1 列附属信息（学号）」；点「一键生成对应桌贴」到 /studio，roster payload headers 只有 `姓名/座位号/排/列/班级`——学号列在排座页被吞后无法带到桌贴。
- 场景：班主任做课桌贴常要「姓名+学号」防同名混淆。当前要在 studio 重新导入一次名单。
- 建议：附属列随 `seatmark.seating-handoff.v1` 透传（排座页不用，但生成桌贴时可用）。

### 【P4｜亲测】EN 首页同一字段两种译法同屏

- /en 首屏 hero 演示标签渲染 `Room 1`（r368 已改），同屏下方「精选模板」卡片仍渲染 `No. 1`（标准考场版卡片 demo 数据）。同一场景字段两种英文并存。

## 二、三条用户流走查结论（与上轮对比）

### 1. 班主任排考场（/seating）【亲测】

- 粘贴纯姓名 30 人、设 6×8、选「隔位」→ 可坐 24、超出提示 + 未排座名单逐项列出；导出 `高三（2）班 期末考试-座位清单.csv` 实测含未排座段（`未排座/序号,姓名`），可直接拿去补排——细节到位。
- 「上传名单文件」现已支持 .xlsx/.xls/.csv/.txt（r219 时仅文本框），学生视角镜像、查找学生（`张`→张伟、张强才命中）、整排交换把手、点选互换撤销均在。
- 结转未修：锁座/封座/固定请假生座位仍无对应控件（遍历按钮 0 命中 锁|固定|钉|封）；按列填充仍未有（间隔项已加，填充顺序仍 按行/S 形两项）。
- 新需求线索（来自真实考务）：**「同班/同组回避」约束**——期中期末排座常要求相邻座位不同班（防串通），当前只有男女混排一类约束；名单若有「班级」列，可加「同班尽量不相邻」选项，与男女混排同机制。
- 演示名单性别错配：见 P2。

### 2. 婚礼策划排桌（/banquet）【亲测】

- 演示 48 人/3 组一键分配成功：拆分解释清楚（「这组 16 人没有一桌能坐下整组，已拆到 2 桌」）、容量预估、换策略入口在；`婚宴座位表-按桌名单.csv`、`婚宴座位表-宾客速查表.pdf`（jsPDF）实测下载。
- Tab/逗号/顿号两列粘贴有完善的「确认名单解析方式」弹窗；空格两列静默错解析（见 P1）。
- 结转未修：**「必须同桌」配对仍无**（界面只有「不同桌」互斥）——双方父母同桌、新人+证婚人同桌、带小孩家庭须同来同坐都需绕路；110 人/5 组超容场景提示清楚（「座位不够：未安排宾客: 35 · 可增加餐桌或提高每桌座位数」），合格。
- 未安排可导出空 CSV 无提示（见 P3）。

### 3. 行政做会议桌牌（/studio）【亲测】

- 粘贴「姓名/职务/单位」名单 → r368 低命中推荐实测触发：「检测到名单更像『讲座嘉宾牌』，切换后可自动映射 3/3 字段」，点「切换」后 3/3 映射、预览即成（王建国/总经理/华诚集团）。该引导有效，上轮 D 类结构卡点已闭环。
- 观察（非缺陷）：推荐落到「讲座嘉宾牌」后每张牌自带固定题头「特邀嘉宾 · DISTINGUISHED GUEST」，内部例会场景用「特邀嘉宾」字样偏隆重——推荐逻辑按字段命中率选没错，但可考虑在职务/单位类名单时优先「会议桌牌/姓名牌」类中性题头模板，或在推荐条里给两个候选。
- 数据表「查看全部数据」支持列排序与漏斗筛选（只读；单元格改写走单张覆写链路）；PNG 导出弹窗水印文案已与实物一致（「细线签名式」描述准确，无 r306 旧文案残留），额度口径「今日剩余 1 次」全站统一（r368 P3a 已修）；带水印导出 zip（`标准考场版-20260908-180552.zip`）实测下载。
- 「2 行字段为空…去查看」数据质量提示在导出弹窗内直接给出，顺手。

### 4. 其余通路【亲测】

- 390px 移动视口 /seating、/studio 均无横向溢出（scrollWidth==innerWidth）；桌面 1280 同。
- /en 站 /seating /banquet /studio /pricing 四页除 ICP 备案号外 0 中文残留；EN 站导航/教程/定价全部英文。
- 页脚 21 条链接 curl 全 200（`/demo` 不存在但页脚「演示数据体验」实际指向 `/studio?demo=1`，无死链）；beian.miit.gov.cn 外链 521 为对方站点行为。
- /pricing 维护态与免费政策文案一致完整（「登录/注册暂不可用（服务维护中）」+「带水印导出与打印不限次」+ 兑换码说明）。
- 反馈浮层按钮存在；点击后未出现 `[role=dialog]`（本轮脚本未命中其容器，未深查，不提交表单——标未验证而非问题）。

## 三、去AI味专项结论

### 总体：站内文案的「AI 腔」已基本清完

- 【代码】仓库已有禁词护栏 `data/__tests__/bannedCopy.spec.ts`（一键生成/全攻略/一站式/保姆级/拉满/效率翻倍/抓手/链路/利器/大幅提升/焕新/组合拳/精雕细琢/零门槛/硬核 等，含「仪式感」限额 ≤3），r349–r368 已把大词清到 0 命中；本轮按同口径 + 「赋能/助力/打造/告别…烦恼/轻松搞定/省时省力/极致/神器/宝藏/综上所述/众所周知/事半功倍/一劳永逸」等词复扫 `guides*.ts`(78 篇)、`templateDetails*`(225 条)、`vsPages.ts`、`topicPages.ts`、各 View 组件：**0 命中**。
- 人工抽读 8 篇教程正文（打印排查、WPS 复刻、毕业典礼座次、档案盒脊、打印店成本、双语桌牌等）：内容以真实痛点开场（「考前一天下午教务把最终编排表发到群里」「打印店的账单由三部分组成」）、给毫米/克重/价格量级等具体数、主动交代边界（哪些准考证不能自己做），是人写的实操文，非排比套话。

### 残余可再改的小处（均为低优先）

| 位置 | 现状 | 问题 | 改写建议 |
|---|---|---|---|
| 首页 FEATURES 区标题/副标 | 「为批量制签和打印交付打磨的细节」/「……——按考场与会场实际打印需求逐项打磨」 | 「打磨」「交付」「逐项打磨」连用两遍，产品经理腔，用户看不懂「打磨」指什么 | 「照着实际打印场景做的细节」；副标可直接列事：「名单解析、字段映射、裁切线与批量导出——按考场、会场的真实打印要求逐项对过」 |
| /seating 演示名单 | 前 24 个用 NAME_GENDERS 池、25 名起按「姓+双字」合成（孙娜玉/梁芳涛/彭平磊…）且性别按序号奇偶 | 合成名越往后越不像真人名，性别错配（见 P2）——演示数据本身有「机器味」 | 扩 NAME_GENDERS 到 ≥60 个带正确性别的常见名，合成兜底时按给定字表分性别选字，或干脆池内循环 |
| /banquet 名单提示 | 「如：张伟 男方亲友」 | 示例写法恰好是实测会错解析的格式（P1） | 修解析或改示例为「张伟，男方亲友（或直接粘贴 Excel 两列）」 |
| 档案盒脊教程标题句 | 「档案室的颜值与效率，都写在盒脊上」 | 轻微营销腔，但在可接受范围 | 可保留；若再收一档：「档案盒侧脊标签怎么做：尺寸、内容与批量打印」 |
| /en 首页 | 同屏 `Room 1` 与 `No. 1` 并存 | 译名不一致（见 P4） | 统一为 `Room 1` |

## 四、新需求候选（供后续轮次排序）

1. /banquet：空格两列名单进「确认名单解析方式」弹窗或改提示示例（P1，建议最高优先）。
2. /seating：「尽量散开」自适应间隔档（30/48 类容量不过半的稀疏排座）。
3. /seating：「同班/同组回避」混排约束（班级列参与相邻回避）。
4. /seating：演示名单改用带正确性别的名字池（P2 顺带解决演示可信度）。
5. /banquet：「必须同桌」结对约束（结转 r365）。
6. /seating：锁座/封座（结转 r365）。
7. /seating→/studio handoff 透传附属列（学号）。
8. /banquet：CSV/速查表导出并入「导出前检查」预检。
9. /en：`No. 1`→`Room 1` 统一（模板卡 demo 数据）。
10. /studio：低命中推荐在职务/单位类名单时优先中性题头模板或给双候选。

## 五、本轮未验证项（如实记录）

- 反馈浮层提交链路、auth 登录链路（维护中）、AI 设计通道：本轮未测。
- 「男女混排」是否真正随机打散：演示名单本身已男女交替，入座后与输入序视觉同构，本轮无法区分「按序交替」与「随机后仍交替」，未验证随机性。
- 照片匹配端到端渲染：沿用前轮「未走完」结论，本轮未重测。
