# 第 234 轮：电子墨水屏（eink）精确像素模式全流程专项（生产，无代码变更轮）

代码依据：pngExport.ts:104-118（6 个 eink 预设 800×480/1280×720/648×480/640×384/400×300/296×128）、:34（自定义宽度校验整数 [100,4096]）、:151（文件名前缀 `{模板名}-{w}x{h}`）、:361（精确像素不写 pHYs）、:44（二值化纯黑白）、EXACT_PIXEL_SUPERSAMPLE=2；PreviewArea.vue:167-283（尺寸模式 exact、预设 select `png-preset`、custom 宽度输入 min/max、预设比例不匹配提示 presetAspectMismatch）、:552（无效宽度 toast「像素宽度无效 请输入 100–4096 之间的整数像素宽度」）；模板 eink800（defaultTemplatesEvent.ts:763，标签 200×120mm，比例 5:3）；入口：HomeView.vue:281 指南链接「电子座签 800×480」→ /guides/eink-800x480-desk-card、templates 库 taxonomy 含 eink800。

## T1 入口可发现性
- 首页含「电子座签 800×480」链接 → 指南页可达且指向 /studio 的 CTA 存在；/templates 库搜「电子座签」找到 eink800 卡片可进 /studio。判据：链接/卡片存在且点击后到达 studio eink800 模板（页面含「电子座签」）。

## T2 分辨率预设全枚举（核心）
- `/studio?template=eink800` 导入 3 行名单（姓名/职务/单位）→ 尺寸模式=精确像素 → 逐个选 6 个预设导出（带水印），解 zip 逐张核验 IHDR 宽高==标称（800×480、1280×720、648×480、640×384、400×300、296×128）；每 zip 3 张同尺寸、文件名含 `-{w}x{h}` 后缀（#151 回归）。
- 注意：eink800 模板比例 5:3，648×480/400×300（4:3）/296×128（2.31:1）预设应出现比例不匹配提示（presetAspectMismatch）——如实记录提示文案与导出行为（拉伸）。

## T3 自定义宽度边界
- custom 预设：输入 100（下限）导出成功高度=round(100*120/200)=60；4096（上限）成功高=2458；99/4097/1.5/负数/空 → toast「像素宽度无效 请输入 100–4096 之间的整数像素宽度」且不产出文件。

## T4 产物质量
- 800×480 产物抽 1 张：chunk 无 pHYs、像素仅 (0,0,0)/(255,255,255) 二值；3 张内容随行变化（姓名不同 → 各张像素 md5 互异）。

## T5 长名/生僻字小尺寸可读性
- 名单含 20 字超长姓名与生僻字「𫔭𨱏」行，296×128 与 800×480 各导出：不报错、卡内文字渲染（非空白），与预览截图对照（#129/#130 口径：预览与导出一致、无溢出裁切异常）；如实记录截断表现。

## T6 全链路回归 + 隐私 + 收尾
- 全程 pageerror=0；Network 捕获中名单标识串命中 0；清 storage 关全部 tab；写 test-report.md 第 234 轮章节。P1/P2 即时上报。
- 取证注记（r170 教训）：每个导出脚本内须重连 browser WS 重设 Browser.setDownloadBehavior，否则落盘为空。
