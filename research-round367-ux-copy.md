# 第 367 轮调研：用户视角走查 + 去AI味专项（只调研不改码）

> 范围：以三种真实用户身份（班主任排考场、婚礼策划排桌、行政做会议桌牌）在 https://www.seatmark.cn 匿名完整走任务流；并审查站内文案/教程/模板演示文字的 AI 腔。
> 证据标注：【亲测】= 本轮用 Playwright 在真实 Chrome 桌面浏览器实际打开/操作生产站验证（含真实下载产物校验）；【直查】= 直接查生产页面文案或仓库源码；【推断】= 由证据推导。
> 铁律遵守：全程匿名，未注册、未登录、未提交反馈/预订表单、未发送真实邮件；仅消耗浏览器本地操作，唯一服务端动作是打开了导出选择弹窗（未点无水印导出，未扣额度）；走查结束后清空浏览器本地存储。
> 生产版本：走查时 rev 头与第 366 轮部署一致（main=aab7c66 之后的生产），首页主包与内文一致。

## 一、三条用户流亲测结果

### 1. 班主任排考场（/seating）【亲测，全程通畅】

- 演示名单一键载入 48 人/48 座；「男女混排」一次成功（toast 报「男 24 / 女 24；相邻座位已男女交替」并附「撤销」按钮），男女生以底色区分。
- 「导出座位清单 .csv」真实下载成功：`高三（2）班 期末考试-座位清单.csv`。
- 座次画布支持点选/拖拽换位、整排交换、教师/学生镜像视角、过道列间隙切换；说明文字把「触屏用点选、桌面可拖拽」讲得清楚。
- 「一键生成对应桌贴」把名单（含座位号/排/列）带入标签工坊，衔接顺畅。
- **卡点 1（中）**：「导出 PNG」按钮旁绿色角标只写「今日剩余 1 次」，不说明是「无水印导出」额度——需悬停 title 才看到完整解释。移动端底部吸底条同样只露「今日剩余 1 次」。首次见到的用户会困惑「什么只剩 1 次？打印还能不能用？」（实际上「打印座位表（A4 横向）」与 CSV 都不占额度，但这个信息只在额度 tooltip / 定价页里）。
- **卡点 2（低）**：座位填充顺序默认「按行填充」，下拉选项语义对第一次用的人偏技术化（行/列/S 形等），无预览示意，需试了才知道效果。

### 2. 婚礼策划排桌（/banquet → /studio）【亲测，惊喜点多】

- 演示名单一键载入 48 位宾客、3 个分组；「一键自动分配」48/48 入座，且把「拆分分组 3」的明细逐条讲清（如「男方亲友 16 人 > 任一桌最大 10 座 → 拆到 1号桌（10 人）、2号桌（6 人）」），并提供「换用『优先坐满』重新分配」一键重试——这是同品类里少见的高透明度分配解释。
- 画布可拖桌、改名、设座位数、加圆桌/长桌/入口/舞台/舞池；「图钉钉住宾客」「不同桌回避」「拖到未安排区撤下」等微调手段齐备。
- 导出侧：「宾客速查表」A4 纵向 PDF 与「按桌名单 .csv」真实下载成功（`婚宴座位表-宾客速查表.pdf`、`婚宴座位表-按桌名单.csv`），且明示「不占无水印次数」。
- 「一键生成席位卡/桌号牌」跳转 `/studio?from=banquet`，名单、分组、桌号全部带入并自动切到婚礼席位卡模板、自动映射 2/2 字段——婚宴 → 席位卡闭环实测打通。
- 带水印导出实测：`婚礼席位卡-20260908-134801.pdf`（5 页 48 枚）下载成功，每张标签底边为细线签名式水印（seatmark.cn 小字），不遮挡内容，符合既定方向。
- **卡点 3（低）**：席位卡上「桌号」字段渲染为「桌号 1号桌」——字段标签「桌号」与值「1号桌」语义重复，读起来像「桌号一号桌」。
- **卡点 4（低）**：自动分配说明里「这组 16 人没有一桌能坐下整组，已拆到 2 桌」这类卡片在画布顶部以展开摘要呈现，信息量大；首次使用者需要读一段才能理解「拆分分组」不是报错。

### 3. 行政做会议桌牌（/studio）【亲测，发现真实缺陷】

- 空态提供「没有文件？粘贴名单」入口，粘贴框支持 Excel 复制的 TSV。
- **卡点 5（中，真实缺陷）**：粘贴 `与会人员/头衔/公司` 这类**非标准表头**的名单时，表头识别未命中（`HEADER_KEYWORDS` 无「与会人员/头衔/公司/嘉宾/职称」等词），表头行被当成数据导入——6 人名单生成 7 张卡，第 1 张是「与会人员」废卡。粘贴框下方虽有「识别到 7 条数据……未检测到表头，首列将按『姓名』处理」+「首行是表头」勾选框可自救，但字迹很小，不仔细看就会带着废卡导出。
- **卡点 6（低）**：已有数据后「粘贴名单」入口消失，只剩「重新上传 / 清空」——想重新粘贴一份修正版需先「清空」，路径不直观。
- 正向：字段映射缺口有醒目警示「有 1 个字段未匹配到名单列，导出时会留空（桌号）+ 去设置映射 →」；225 款模板画廊支持拼音/首字母搜索与两级场景筛选；「单张覆写」可单独改某一张；「按不干胶纸型选择」会按模板尺寸推荐最接近的市售纸型。
- 可视化设计器、模板分享链接、微信扫码打开、导出 JSON 均可用。

### 4. 其他观察

- 【亲测】移动端 390px 视口下 /、/seating、/banquet、/studio 均无横向溢出；/seating 有吸底「查看座位预览 ↓ / 下一步：检查并导出」条。
- 【亲测】意见反馈浮层（功能建议/问题反馈/其他 + 选填联系方式）正常打开——未提交。
- 【直查】定价页与规则一致：专业版原价 ¥19、团队版 ¥49、限时 0 折免费、不接真实支付、兑换码过渡；但「登录/注册暂不可用（服务维护中）」同一段提示在页内出现 3 次（顶栏、专业版卡、FAQ 顶部），视觉重复。
- 【直查】英文站整体翻译质量在线；但首屏演示数据中「考场」字段值为 `No. 1`（views/HomeView.vue `考场: 'No. ${n}'`），而站内其他英文文案用 `Room 1`——「No. 1」在英文里读起来像名次而非房间号，建议统一为 `Room 1`。
- 【亲测】关闭页面重开后 /studio 仍保留上次导入的「粘贴的名单」——本地草稿持久化按设计工作；文末已清理浏览器存储。

## 二、去AI味专项审查

### 总体结论

经第 349–364 轮多轮治理（bannedCopy.spec.ts 已建 25+ 禁词护栏 + 限额词），**站内长文案（教程、对比页、模板详情 intro）的 AI 腔已基本清除**：78 篇教程结尾无「希望这份指南」「相信你一定」式公式收尾（全文检 0 命中套话收尾模板）；指南正文多为场景化写法（如「考前一天下午，教务把最终考场编排表发到群里」），含具体数字与可验证建议；vs 对比页措辞克制（「对方领先项照写」「以其官网最新信息为准」）。

### 仍残留的结构性 AI 痕迹【直查源码量化】

- **225 款模板一句话描述（`description`）是最大残留面**：116/225（52%）使用「+」连缀枚举（「特大字 + XX 与 YY」），27 条含「特大字/超大字」，11 条含「一眼X」，22 条以「通用/醒目/皆宜/吸睛」式功能短语收尾。单条看合格，**批量浏览时句式骨架完全一致**（「[规格]，[元素]+[元素]，[场景/效果]」），是站内最像机器批量生成的地方。建议下一轮抽查改写 20–30 条，把「+」枚举改成自然语句、删掉模板化尾缀（「通用」「皆宜」「一眼」各保留少量即可），并考虑把「句式骨架多样性」做成可测的 spec（例如同一前缀/尾缀不超过 N 条）。
- 模板名质量尚可，无生造词；演示数据姓名经 `sampleNamesRealistic.spec` 约束，自然。

### 具体可改写示例（低优先，仅作样例）

| 位置 | 现状 | 建议 |
|---|---|---|
| 席位卡「桌号」字段 | 「桌号 1号桌」 | 值已含「桌」，标签可省略或渲染为「1号桌」 |
| 英文首屏演示 | `No. 1`（考场） | 统一为 `Room 1`（与 en.ts 其他处一致） |
| 定价页维护提示 | 同句出现 3 次 | 保留顶部 1 处 + FAQ 内 1 处即可 |
| 额度角标 | 「今日剩余 1 次」 | 按钮/吸底条上补全为「无水印导出今日剩余 1 次」或加 ? 图标说明 |

### 未发现的问题

- 空洞套话排比（「不仅…而且…更」「赋能/助力/抓手」）：全量数据文件 grep 0 命中（已有护栏）。
- 公式化开头/结尾（「在数字化时代」「总而言之」「希望本文」）：0 命中。
- 夸大不可验证表述（「神器」「效率翻倍」「告别XX」）：禁词表已覆盖。

## 三、汇总：卡点与建议优先级

1. **P1 — 粘贴导入非标表头静默当数据**（卡点5）：扩充 `HEADER_KEYWORDS`（与会人员|头衔|职称|公司|嘉宾|代表|科室|系别|年级|组别 等），或在「未检测到表头」且首行全为非名词性短词时，把表头判断提示升级为更显眼的条内警告（当前为 11px 灰字）。
2. **P2 — 额度角标语义不全**（卡点1）：「今日剩余 1 次」需补「无水印导出」主语，尤其移动端吸底条；并在角标处明示「打印/带水印导出不限次」。
3. **P3 — 模板描述批量句式同构**（去AI味主战场）：见第二节量化数据与改写建议。
4. **P4 — 小项**：桌号字段标签冗余、EN 演示 `No. 1`→`Room 1`、定价页维护提示去重、有数据态下补回「粘贴名单」入口。
